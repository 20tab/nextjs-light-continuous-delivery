apiVersion: apps/v1
kind: Deployment
metadata:
  name: integration-{{cookiecutter.project_slug}}-frontend-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      component: integration-{{cookiecutter.project_slug}}-frontend
  template:
    metadata:
      labels:
        component: integration-{{cookiecutter.project_slug}}-frontend
    spec:
      imagePullSecrets:
        - name: regcred
      containers:
        - name: frontend
          image: registry.gitlab.com/{{cookiecutter.gitlab_group_name}}/{{cookiecutter.project_dirname}}:__IMAGE_TAG__
          ports:
            - containerPort: 3000
          # env:
          #   - name: API_URL
          #     valueFrom:
          #       secretKeyRef:
          #         name: integration-{{cookiecutter.project_slug}}-secrets
          #         key: API_URL

---

apiVersion: v1
kind: Service
metadata:
  name: integration-{{cookiecutter.project_slug}}-frontend-cluster-ip-service
spec:
  type: ClusterIP
  selector:
    component: integration-{{cookiecutter.project_slug}}-frontend
  ports:
    - port: 3000
      targetPort: 3000
